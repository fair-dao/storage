# Контракт хранилища пользовательских данных FAIR DAO

[Английский](README.md)  ·  [Китайский(упрощенный)](README_CN.md)  ·  [Русский](README_RU.md)  ·  [Español](README_ES.md)  ·  [Français](README_FR.md)  ·  [العربية](README_AR.md)

## Введение
Контракт репозитория данных пользователей — это основной сервис хранения данных бэкэнда FAIR DAO для других смарт-контрактов. Он предоставляет безопасные и гибкие возможности хранения данных с комплексными механизмами контроля доступа.

## Основные функции

### 1. Управление менеджерами
- **Добавление/удаление менеджеров**: Владельцы могут добавлять или удалять менеджеры с настраиваемыми разрешениями
- **Ролевой доступ**: Разделяет обычных менеджеров и владельцев с повышенными привилегиями

### 2. Управление ключами
- **Назначение менеджеров ключей**: Назначать определенных менеджеров для контроля доступа к конкретным ключам
- **Пакетное назначение менеджеров ключей**: Эффективно устанавливать менеджеры для нескольких ключей одновременно
- **Трассировка ключей**: Поддерживать индекс всех зарегистрированных ключей

### 3. Функции хранения данных
- **Хранение данных, специфичных для пользователя**: Хранить и извлекать данные, связанные с конкретными адресами пользователей
- **Общее хранилище данных**: Хранить и извлекать данные, доступные нескольким контрактам/пользователям
- **Отслеживание меток времени**: Автоматически записывать метки времени для всех изменений данных

### 4. Безопасность
- **Аварийная остановка**: Приостанавливать критические операции контракта в случае безопасности
- **Проверка разрешений**: Строгий контроль доступа для всех чувствительных операций

### 5. Запросы
- **Информация о менеджерах**: Получать списки менеджеров и их разрешений
- **Информация о ключах**: Доступ к зарегистрированным ключам и назначенным менеджерам
- **Состояние контракта**: Проверка, находится ли контракт в режиме аварийной остановки

### 6. Операции с токенами
- **Зачисление токенов**: Пополнение баланса токенов пользователя (только менеджер ключа)
- **Вывод токенов**: Снятие токенов с баланса пользователя (только менеджер ключа)
- **Проверка баланса**: Просмотр баланса токенов пользователя (открытый доступ)
- **Выпуск событий**: Генерация событий для всех переводов токенов для прозрачного отслеживания
- **Передача управления ключом**: Передача прав управления ключом другому адресу (только менеджер ключа)

## Функции контракта

### Управление менеджерами
- `addManager(address manager, bool withOwnerPermission)`: Добавить нового менеджера с необязательными разрешениями владельца
- `removeManager(uint256 index, address manager)`: Удалить существующего менеджера
- `isManager(address user)`: Проверить, является ли адрес менеджером
- `getOwnerCount()`: Получить количество владельцев
- `getManagerCount()`: Получить общее количество менеджеров

### Управление ключами
- `addKeyManagers(bytes32[] keys, address manager)`: Добавить менеджера для нескольких ключей (только менеджер) - работает только если ключи не имеют существующего менеджера
- `isKeyManager(bytes32 key, address user)`: Проверить, является ли адрес менеджером для определенного ключа
- `getKeyAtIndex(uint256 index)`: Получить ключ по определенному индексу

### Операции с данными
- `setUserData(address targetUser, bytes32 key, bytes data)`: Хранить данные для определенного пользователя
- `getUserData(address targetUser, bytes32 key)`: Извлечь данные, специфичные для пользователя
- `setSharedData(bytes32 key, bytes32 sharedValueId, bytes data)`: Хранить общие данные
- `getSharedData(bytes32 key, bytes32 sharedValueId)`: Извлечь общие данные

### Операции с токенами
- `depositTokens(address user, bytes32 key, uint256 amount)`: Пополнить баланс токенов пользователя (только менеджер ключа)
- `withdrawTokens(address user, bytes32 key, uint256 amount)`: Снять токены с баланса пользователя (только менеджер ключа)
- `getTokenBalance(address user, bytes32 key)`: Просмотреть баланс токенов пользователя (открытый доступ)
- `transferKeyManagement(bytes32 key, address newManager)`: Передача управления ключом другому адресу (только менеджер ключа)

### События
- `AddKeyManager(bytes32 indexed key, address indexed manager, address writer)`: Генерируется при добавлении менеджера ключа
- `TransferKeyManager(bytes32 indexed key, address indexed oldManager, address indexed newManager, address writer)`: Генерируется при передаче управления ключом
- `TokenDeposited(address user, bytes32 key, uint256 amount, address operator)`: Генерируется при зачислении токенов
- `TokenWithdrawn(address user, bytes32 key, uint256 amount, address operator)`: Генерируется при снятии токенов
- `EmergencyStopped(address operator)`: Генерируется при включении аварийной остановки
- `EmergencyResumed(address operator)`: Генерируется при отключении аварийной остановки

### Безопасность
- `enableEmergencyStop()`: Приостановить критические операции контракта
- `disableEmergencyStop()`: Возобновить операции контракта
- `isEmergencyStopped()`: Проверить, находится ли контракт в режиме аварийной остановки

## Вклад

* Мы приветствуем отправку PR или отчетов о проблемах, пожалуйста, обращайтесь к [Руководству по вкладу](https://github.com/fair-dao/.github/blob/main/CONTRIBUTING_RU.md).
* **При внесении вклада вы можете оставить свой адрес кошелька TRON (по крайней мере один раз), и мы будем оценивать ваш уровень вклада каждый квартал и распределять токены Fair в качестве вознаграждения за активных участников.**

## Лицензия

* Все права защищены (c) 2025 FAIR DAO. Все права защищены.
* Лицензировано в соответствии с GNU General Public License Версия 3 ( [GPLv3](LICENSE) ).